[project]
name = "legends-vote"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.47",
    "python-dotenv>=1.0",
    "winotify>=1.1.0 ; sys_platform == 'win32'",
    "pyinstaller>=6.0",
]

[tool.uv]
package = false